 ## Running the server
  node index.js (from server/)

  ## Testing multi-user
  1. Run server: node index.js
  2. Run client: npm run dev (from client/)
  3. Open two tabs at localhost:5173
  4. Tab 1: Create Room, copy the room code
  5. Tab 2: Paste code into Room ID input, click Join Room
  6. Tab 1: Click Broadcast — both tabs should receive the event